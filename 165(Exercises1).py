def proc(x):
    return x + 2
def proc(n):
    return 2*n + 1 # Its not a legal python program because we use a wrong def main x = proc(5) here we
# should be use x = proc(n)
def main():
    x = proc(5)
main()
