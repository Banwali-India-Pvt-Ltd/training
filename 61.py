# gat two integers from the user
dividend, divisor = eval(input('please enter two numbers to divide: '))
# if possible, divide them and report the resuit
if divisor !=0:
    quotient = dividend/divisor
    print(dividend, '/', dividend, '=', quotient)
print('program finished')
